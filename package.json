{
    "name": "rest-node-server",
    "description": "Contains the flows for the REST node Node-Red server",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}